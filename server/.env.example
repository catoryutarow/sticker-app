# Encoder Server - Environment Variables

# サーバーポート（デフォルト: 3001）
PORT=80

# 許可するオリジン（カンマ区切り）
# 本番環境では必ず設定すること
# 例: https://your-app.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# JWT認証用シークレット（本番環境では必ず強力なランダム文字列を設定）
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# 環境（production/development）
NODE_ENV=development

# ================================
# メール送信設定（Resend）
# ================================

# Resend APIキー（https://resend.com で取得）
# 設定しない場合、メール認証機能はスキップされます
# RESEND_API_KEY=re_xxxxxxxxxxxxx

# メール送信元アドレス（Resendで認証されたドメインが必要）
# EMAIL_FROM=シール帳 <noreply@your-domain.com>

# フロントエンドURL（メール内リンク用）
# FRONTEND_URL=https://your-domain.com

# ================================
# 管理者自動作成（オプション）
# ================================

# 初回起動時に管理者ユーザーを自動作成
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=your-secure-admin-password
