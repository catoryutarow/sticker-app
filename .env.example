# Sticker App - Environment Variables
#
# 環境別の設定は以下のファイルで自動切り替え:
# - .env.development  → npm run dev 時
# - .env.production   → npm run build 時
# - .env.local        → 全環境（gitignore推奨、個人設定用）

# エンコードサーバーのURL（Vercelデプロイ時に設定）
# 例: https://sticker-encoder.fly.dev
VITE_ENCODER_URL=

# API URL（必須）
# 開発: http://localhost:3001/api
# 本番: https://api.your-domain.com/api
VITE_API_URL=

# アセット（画像・音声）のベースURL
# 未設定の場合はローカルパス（/assets, /backgrounds）を使用
# 例: https://d123456.cloudfront.net
VITE_ASSET_BASE_URL=
