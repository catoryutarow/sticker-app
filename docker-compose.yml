services:
  encoder:
    build: ./server
    ports:
      - "3001:3001"
    volumes:
      - ./server/data:/app/data
      - ./public:/data/public
    env_file:
      - ./server/.env
    environment:
      - UPLOAD_DIR=/data
    restart: unless-stopped
